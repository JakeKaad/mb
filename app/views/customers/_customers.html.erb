<%= link_to "Add another customer to event", new_event_customer_path(event), remote: true, class: "btn btn-success btn-sm" %>
<% customers.each do |customer| %>
  <%= render 'customers/customer', customer: customer, event: event %>
<% end %>
