language: ruby
rvm:
  - 2.3.0
bundler_args: --without production
gemfile:
  - rails/Gemfile
before_install:
  - gem update --system
  - gem --version
script:
  - bundle exec rake -f rails/Rakefile db:create
  - bundle exec rake -f rails/Rakefile db:migrate
  - bundle exec rspec
